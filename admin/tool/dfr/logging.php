<?php

//TODO
//moodle already logs and stores data under mdl_logstore_standard_log quite extensively. for now, the plugin has its own table and
//not a separate external database (but consider for later). necessary to store this data duplicately? maybe if need for hashed.